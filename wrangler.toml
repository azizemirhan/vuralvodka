# Cloudflare Workers — static site (HTML, medya, görseller)
# https://developers.cloudflare.com/workers/static-assets/
#
# Dağıtım: npx wrangler deploy
# (İlk sefer: npx wrangler login)

name = "vuralvodka"
compatibility_date = "2025-10-15"

# Statik site (proje kökünden ayrı; node_modules yüklenmez)
[assets]
directory = "public"
